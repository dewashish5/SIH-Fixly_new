# Local secrets — copy to `.env` and fill values.
# Never commit `.env`.

# Mapbox public token (pk.*)
Mapbox_api_key=pk.YOUR_MAPBOX_PUBLIC_TOKEN

# Backend API (Cloudflare tunnel URL from bakend-master PUBLIC_URL)
API_BASE_URL=https://tulsa-michigan-proved-rap.trycloudflare.com
