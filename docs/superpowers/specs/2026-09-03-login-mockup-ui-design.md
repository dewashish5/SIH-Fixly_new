# Login screen UI → mockup layout

Keep Fixly theme colors (`ColorScheme` / `AppColors`). Change login layout only.

## Layout
1. Full-bleed diagonal gradient header (primary → primaryDark), large white "Login"
2. Surface card with oversized top-left radius (~88), fills rest
3. Underline Username (email) + Password fields with labels
4. Forgot Password right-aligned
5. Row: Remember Me checkbox | Sign In button (primaryDark)
6. Bottom: Google square | "or" | Apple square (visual; Apple → snackbar if unsupported)
7. Keep worker chip + sign-up link below (app needs them; mockup omits)

## Out of scope
Theme tokens, signup page, new auth providers
