[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gig_worker_reliability"
version = "1.0.0"
description = "AI Model for Gig Worker Reliability Scoring and Quality Monitoring"
readme = "README.md"
requires-python = ">=3.8"
